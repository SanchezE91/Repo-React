const ItemListContainer = ({mensaje}) => {
    return (
        <div className="item-container">
            <h2>{mensaje}</h2>
            <p>Estamos trabajando para traerte los mejores productos al mejor precio.</p>
        </div>
    )
}

export default ItemListContainer